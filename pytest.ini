[pytest]
; The game code lives in src/ (spec 00: Project architecture). We use a flat
; src layout without packaging metadata for now - future specs can add a
; proper package manifest when an entry point is needed.
pythonpath = src
testpaths = tests
