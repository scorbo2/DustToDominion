# Dust to Dominion

This is a work in progress. User-facing documentation to follow.

## Setup instructions

(TODO: walk through initial installation)

Something like `pip install -r requirements.txt` inside a venv...
TODO: make sure we have a `requirements.txt` with pygame-ce, loguru, etc.

## Testing instructions

(TODO: walk through how to run pytest)

## Configuration

(TODO: walk through the main game config file with examples)

